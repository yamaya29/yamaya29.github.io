const PHASE_DOCUMENT_LINKS = {
  "DT - Reconocimiento del territorio.pdf":
    "https://drive.google.com/file/d/1tpJH5R5B7sOUy0Vh2kODIlB7jZTbl7xs/view?usp=drivesdk",
  "ABP - Guía 2.pdf":
    "https://drive.google.com/file/d/1Q-dRclLBUBc1Z3bfNptH7qFHCk3p83c9/view?usp=drivesdk",
  "TALLER METODOLÓGICO - Comunicación Científica para estudiantes por el dinamizador.docx":
    "https://docs.google.com/document/d/11vZMRi4Jr0OEJHPhcLFNPYl_BHGYmINA/edit?usp=sharing&ouid=100573075960276525015&rtpof=true&sd=true",
  "ABP - Guía 5.pdf":
    "https://drive.google.com/file/d/1gYBmIyAmwTAe0-EDdnIOFFISyBMcVbeM/view?usp=drivesdk",
  "DT - Empatía y actores.pdf":
    "https://drive.google.com/file/d/1MjdstGhisbzzst9aq588fLUrEfHYJ8By/view?usp=drivesdk",
  "EAFIT Semilleros - Formular preguntas de investigación.pdf":
    "https://drive.google.com/file/d/1x--xlq2G539KUY65-Y_jIDIxRjO4zGA5/view?usp=drivesdk",
  "TALLER METODOLÓGICO - Storytelling  para estudiantes por el dinamizador.docx":
    "https://docs.google.com/document/d/1lCGgyxMuotCvw3N9fQOupJfRAIQcxWdt/edit?usp=sharing&ouid=100573075960276525015&rtpof=true&sd=true",
  "DT - Ideación divergente.pdf":
    "https://drive.google.com/file/d/1CSfxGodTAyUSiQUUaSxFrZJ3BCdClLzN/view?usp=drivesdk",
  "DT - Priorización de ideas.pdf":
    "https://drive.google.com/file/d/1y-HwOTRqJkWTlGNmdQP-Fp6kZP5p1rnO/view?usp=drivesdk",
  "ABP - Guía 4.pdf":
    "https://drive.google.com/file/d/17rM3lKlXCCPTdjBF6EsEo5LBE_sBjZHn/view?usp=drivesdk",
  "ABP - Guía 6.pdf":
    "https://drive.google.com/file/d/1ob4gdwEVjbTxS2KxsPystW6mYVQUjy7r/view?usp=drivesdk",
  "TALLER METODOLÓGICO - Comunicación Responsable para estudiantes por el dinamizador.docx":
    "https://docs.google.com/document/d/1EmjpUpSDU30tSO2K79uGxBhFn5HrFzBX/edit?usp=sharing&ouid=100573075960276525015&rtpof=true&sd=true",
  "EAFIT Semilleros - Plan de recolección de datos.pdf":
    "https://drive.google.com/file/d/1RF9zkkzbUrVv7yDG_la953JBVbyxQgEc/view?usp=drivesdk",
  "EAFIT Semilleros - Primer experimento.pdf":
    "https://drive.google.com/file/d/1cglED1K6-qTFlgyaBtWzVf8cG_j2uFg3/view?usp=drivesdk",
  "Programación - Sesión 6.pdf":
    "https://drive.google.com/file/d/1u15trnaaC3W68Uc4Id4Ny7JNBT3V3EmK/view?usp=drivesdk",
  "EAFIT Semilleros - Sistematización y análisis de datos.pdf":
    "https://drive.google.com/file/d/1Me_CAY08wdBNAwFMaz9C05K9r5hxJ4Kg/view?usp=drivesdk",
  "Programación - Sesión 10.pdf":
    "https://drive.google.com/file/d/1sXMKC7Y01swLHVVFyuGpnmc5Ax7991sZ/view?usp=drivesdk",
  "ABP - Guía 10.pdf":
    "https://drive.google.com/file/d/1y4yw86EBuRVC2bmZhW0Ci7LMTqBJbhA0/view?usp=drivesdk",
  "DT - Prototipo inicial.pdf":
    "https://drive.google.com/file/d/14gxYT4ddSaF1xzDOccu5G7q4XnpoZk4x/view?usp=drivesdk",
  "DT - Prueba del prototipo.pdf":
    "https://drive.google.com/file/d/1jkpOPZGyCmJolST3XyKXVNbNAQ547seP/view?usp=drivesdk",
  "TALLER METODOLÓGICO - INFOGRAFÍAS para estudiantes por el dinamizador.docx":
    "https://docs.google.com/document/d/1auD5-sKXFfLxAv8eJwuEqvvhOdJrG44-/edit?usp=sharing&ouid=100573075960276525015&rtpof=true&sd=true",
  "Programación - Sesión 5.pdf":
    "https://drive.google.com/file/d/1KVZ0nxHR6tuSn-9ybZ4MxSCR4mXNKLmP/view?usp=drivesdk",
  "Programación - Sesión 9.pdf":
    "https://drive.google.com/file/d/11mhEOCHFhOSzWdZwGAEBJvmfOJW_ZGbH/view?usp=drivesdk",
  "DT - Validación con usuarios.pdf":
    "https://drive.google.com/file/d/1ekQ3P6m3eS56r18SDDFVwHSSU-wI8mI0/view?usp=drivesdk",
  "EAFIT Semilleros - Organizamos la información.pdf":
    "https://drive.google.com/file/d/1n98-esohA4VNrEYk8PNYHaDkAMgfb1VX/view?usp=drivesdk",
  "EAFIT Semilleros - Listos para presentar nuestra solución.pdf":
    "https://drive.google.com/file/d/10V4UoIpa9KIVPBRxTrEV-yzvt6aqAtui/view?usp=drivesdk",
  "EAFIT Semilleros - Preparamos presentaciones.pdf":
    "https://drive.google.com/file/d/1HV---xNEAjEweiZ0nrr7Zgqeo05zYIKm/view?usp=drivesdk",
  "ABP - Guía 11.pdf":
    "https://drive.google.com/file/d/1T__EkFzOniX2ptx4uX4qk1Pdxab1uSaV/view?usp=drivesdk",
  "Programación - Sesión 11.pdf":
    "https://drive.google.com/file/d/1dXnWPhYLK6zjNuL_VFZUYDX19q17-_4w/view?usp=drivesdk",
  "TALLER METODOLÓGICO Narrativas para estudiantes por el dinamizador.docx":
    "https://docs.google.com/document/d/1WAGBeHaLahBOUgYxqLNpgJaq1lA8-P4c/edit?usp=sharing&ouid=100573075960276525015&rtpof=true&sd=true",
  "EAFIT Programación - Presentación final y celebración.pdf":
    "https://drive.google.com/file/d/1SFJuJUpadEABm2_r7WGMfZwoJyrVIT3b/view?usp=drivesdk",
  "Programación - Sesión 12.pdf":
    "https://drive.google.com/file/d/1cBYRZC2ikihxGJ7NjLedMP_ylhJgh821/view?usp=drivesdk",
  "EAFIT Semilleros - Socializamos con la comunidad.pdf":
    "https://drive.google.com/file/d/1LLBm0oiJfZf_mq4qoPiAaM5svtZ8iAuY/view?usp=drivesdk",
  "ABP - Guía 12.pdf":
    "https://drive.google.com/file/d/1F1emz5-p317PA7G0vp2Oqg5r9GPu6SDH/view?usp=drivesdk",
};

const JOURNEY_LAYOUT = {
  desktopPoints: [
    { x: 20, y: 14 },
    { x: 73, y: 31 },
    { x: 28, y: 51 },
    { x: 74, y: 71 },
    { x: 38, y: 89 },
  ],
  mobilePoints: [
    { x: 34, y: 14 },
    { x: 66, y: 32 },
    { x: 38, y: 52 },
    { x: 66, y: 72 },
    { x: 40, y: 90 },
    { x: 70, y: 98 },
  ],
};

const ADDITIONAL_MATERIALS = [
  {
    id: "infografias",
    label: "Material adicional",
    title: "Infografías",
    description:
      "Accede a las infografías organizadas por fase. Por ahora los enlaces son temporales mientras se comparte la versión final.",
    links: [
      { label: "Fase 1", href: "#" },
      { label: "Fase 2", href: "#" },
      { label: "Fase 3", href: "#" },
      { label: "Fase 4", href: "#" },
      { label: "Fase 5", href: "#" },
    ],
  },
  {
    id: "instrumentos-valoracion",
    label: "Material adicional",
    title: "Instrumentos de valoración",
    description:
      "Aquí aparecerán los instrumentos de evaluación por fase. De momento dejamos los accesos placeholder para completar luego.",
    links: [
      { label: "Fase 1", href: "#" },
      { label: "Fase 2", href: "#" },
    ],
  },
];

function escapeHtml(value) {
  return String(value)
    .replaceAll("&", "&amp;")
    .replaceAll("<", "&lt;")
    .replaceAll(">", "&gt;")
    .replaceAll('"', "&quot;");
}

function normalizeText(value) {
  return String(value || "").trim();
}

function formatMultiline(text) {
  if (!text) {
    return "";
  }

  return text
    .split("\n")
    .map((line) => line.trim())
    .filter(Boolean)
    .map((line) => `<span>${escapeHtml(line)}</span>`)
    .join("");
}

function buildSmoothPath(points) {
  if (points.length < 2) {
    return "";
  }

  let path = `M ${points[0].x} ${points[0].y}`;
  for (let index = 1; index < points.length; index += 1) {
    const previous = points[index - 1];
    const current = points[index];
    const controlX = (previous.x + current.x) / 2;
    const controlY = (previous.y + current.y) / 2;
    path += ` Q ${previous.x} ${previous.y} ${controlX} ${controlY}`;
  }

  const last = points[points.length - 1];
  path += ` T ${last.x} ${last.y}`;
  return path;
}

function renderStats(target, phases) {
  const totalSessions = phases.reduce((sum, phase) => sum + phase.sessionCount, 0);
  target.innerHTML = `
    <article class="stat-card">
      <strong>${phases.length}</strong>
      <span>fases</span>
    </article>
    <article class="stat-card">
      <strong>${totalSessions}</strong>
      <span>sesiones</span>
    </article>
  `;
}

function renderBoard(board, phases) {
  const points = window.matchMedia("(max-width: 760px)").matches
    ? JOURNEY_LAYOUT.mobilePoints
    : JOURNEY_LAYOUT.desktopPoints;
  const path = buildSmoothPath(points);
  const svg = board.querySelector("[data-route-svg]");
  const stationsHost = board.querySelector("[data-journey-stations]");

  svg.setAttribute("viewBox", "0 0 100 100");
  svg.setAttribute("preserveAspectRatio", "none");
  svg.innerHTML = `
    <path class="route-guide" d="${path}" pathLength="100"></path>
    <path class="route-dots" d="${path}" pathLength="100"></path>
  `;

  stationsHost.innerHTML = phases
    .map((phase, index) => {
      const point = points[index];
      return `
        <button
          class="station station-tone-${phase.number}"
          type="button"
          data-phase-id="${phase.id}"
          aria-label="${phase.name}"
          style="left:${point.x}%; top:${point.y}%"
        >
          <span class="station-label">${phase.label}</span>
          <strong>${escapeHtml(phase.title)}</strong>
          <span class="station-meta">${phase.sessionCount} sesiones</span>
        </button>
      `;
    })
    .join("");
}

function renderAdditionalMaterials(host, materials) {
  if (!host) {
    return;
  }

  host.innerHTML = materials
    .map((material, index) => {
      const itemLabel = material.links.length === 1 ? "recurso" : "recursos";
      return `
        <button
          class="resource-card resource-tone-${(index % 2) + 1}"
          type="button"
          data-resource-id="${material.id}"
          aria-label="${escapeHtml(material.title)}"
        >
          <span class="resource-card-label">${escapeHtml(material.label)}</span>
          <strong>${escapeHtml(material.title)}</strong>
          <p>${escapeHtml(material.description)}</p>
          <span class="resource-card-meta">${material.links.length} ${itemLabel}</span>
        </button>
      `;
    })
    .join("");
}

function makeDocumentsMarkup(documents) {
  if (!documents.length) {
    return `<p class="empty-copy">No hay documento referenciado para esta sesión.</p>`;
  }

  return `
    <ul class="document-list">
      ${documents
        .map((documentName) => {
          const label = normalizeText(documentName);
          const href = PHASE_DOCUMENT_LINKS[label];
          const tag = label.toLowerCase().endsWith(".docx") ? "DOCX" : "PDF";

          if (!href) {
            return `
              <li>
                <span class="document-link is-muted">
                  <span class="document-tag">${tag}</span>
                  ${escapeHtml(label)}
                </span>
              </li>
            `;
          }

          return `
            <li>
              <a
                class="document-link"
                href="${escapeHtml(href)}"
                target="_blank"
                rel="noreferrer noopener"
              >
                <span class="document-tag">${tag}</span>
                ${escapeHtml(label)}
              </a>
            </li>
          `;
        })
        .join("")}
    </ul>
  `;
}

function makeResourceLinksMarkup(links) {
  if (!links.length) {
    return `<p class="empty-copy">No hay enlaces disponibles todavía.</p>`;
  }

  return `
    <ul class="document-list">
      ${links
        .map((link) => {
          const href = normalizeText(link.href) || "#";
          const disabled = href === "#";

          if (disabled) {
            return `
              <li>
                <a
                  class="document-link is-muted"
                  href="#"
                  aria-disabled="true"
                  data-placeholder-link
                >
                  <span class="document-tag">Pronto</span>
                  ${escapeHtml(link.label)}
                </a>
              </li>
            `;
          }

          return `
            <li>
              <a
                class="document-link"
                href="${escapeHtml(href)}"
                target="_blank"
                rel="noreferrer noopener"
              >
                <span class="document-tag">Link</span>
                ${escapeHtml(link.label)}
              </a>
            </li>
          `;
        })
        .join("")}
    </ul>
  `;
}

function makeSessionMarkup(session, isOpen) {
  const objective = normalizeText(session.objective);
  const activity = normalizeText(session.activity);
  const pages = normalizeText(session.pages);
  const skills = Array.isArray(session.skills) ? session.skills : [];

  return `
    <details class="session-card"${isOpen ? " open" : ""}>
      <summary>
        <span>${escapeHtml(session.title)}</span>
        <span class="session-chevron" aria-hidden="true"></span>
      </summary>
      <div class="session-content">
        ${
          objective
            ? `
              <section class="detail-block detail-objective">
                <h4>Objetivo</h4>
                <p>${escapeHtml(objective)}</p>
              </section>
            `
            : ""
        }
        ${
          skills.length
            ? `
              <section class="detail-block detail-skills">
                <h4>Habilidades del siglo XXI</h4>
                <div class="chip-row">
                  ${skills.map((skill) => `<span class="chip">${escapeHtml(skill)}</span>`).join("")}
                </div>
              </section>
            `
            : ""
        }
        ${
          activity
            ? `
              <section class="detail-block detail-activity">
                <h4>Actividad recomendada</h4>
                <div class="rich-copy">${formatMultiline(activity)}</div>
              </section>
            `
            : ""
        }
        ${
          pages
            ? `
              <section class="detail-block detail-pages">
                <h4>Páginas</h4>
                <p>${escapeHtml(pages)}</p>
              </section>
            `
            : ""
        }
        <section class="detail-block detail-documents">
          <h4>Documento referenciado</h4>
          ${makeDocumentsMarkup(session.documents || [])}
        </section>
      </div>
    </details>
  `;
}

function setupModal(page, phases, materials) {
  const modal = page.querySelector("[data-phase-modal]");
  const title = modal.querySelector("[data-modal-title]");
  const description = modal.querySelector("[data-modal-description]");
  const counter = modal.querySelector("[data-modal-count]");
  const sessionsHost = modal.querySelector("[data-modal-sessions]");
  const linksHost = modal.querySelector("[data-modal-links]");
  const closeButton = modal.querySelector("[data-close-modal]");
  let lastTrigger = null;

  function closeModal() {
    modal.hidden = true;
    document.body.classList.remove("modal-open");
    if (lastTrigger) {
      lastTrigger.focus();
    }
  }

  function openModal(phase, trigger) {
    lastTrigger = trigger;
    title.textContent = phase.name;
    description.textContent = phase.description;
    counter.textContent = `${phase.sessionCount} sesiones`;
    linksHost.hidden = true;
    linksHost.innerHTML = "";
    sessionsHost.hidden = false;
    sessionsHost.innerHTML = phase.sessions
      .map((session, index) => makeSessionMarkup(session, index === 0))
      .join("");
    const renderedDetails = sessionsHost.querySelectorAll(".session-card");
    renderedDetails.forEach((details) => {
      details.addEventListener("toggle", () => {
        if (details.open) {
          enforceSingleOpenSession(details);
        }
      });
    });
    modal.hidden = false;
    document.body.classList.add("modal-open");
  }

  function openResourceModal(material, trigger) {
    lastTrigger = trigger;
    title.textContent = material.title;
    description.textContent = material.description;
    counter.textContent = `${material.links.length} ${material.links.length === 1 ? "recurso" : "recursos"}`;
    sessionsHost.hidden = true;
    sessionsHost.innerHTML = "";
    linksHost.hidden = false;
    linksHost.innerHTML = makeResourceLinksMarkup(material.links);
    modal.hidden = false;
    document.body.classList.add("modal-open");
  }

  function enforceSingleOpenSession(openDetails) {
    const allDetails = sessionsHost.querySelectorAll(".session-card");
    allDetails.forEach((details) => {
      if (details !== openDetails) {
        details.open = false;
      }
    });
  }

  if (!page.dataset.boundEvents) {
    page.addEventListener("click", (event) => {
      const trigger = event.target.closest("[data-phase-id]");
      if (trigger) {
        const phase = phases.find((item) => item.id === trigger.dataset.phaseId);
        if (phase) {
          openModal(phase, trigger);
        }
        return;
      }

      const resourceTrigger = event.target.closest("[data-resource-id]");
      if (resourceTrigger) {
        const material = materials.find((item) => item.id === resourceTrigger.dataset.resourceId);
        if (material) {
          openResourceModal(material, resourceTrigger);
        }
        return;
      }

      if (event.target.closest("[data-placeholder-link]")) {
        event.preventDefault();
      }
    });

    closeButton.addEventListener("click", closeModal);

    modal.addEventListener("click", (event) => {
      if (event.target === modal) {
        closeModal();
      }
    });

    window.addEventListener("keydown", (event) => {
      if (event.key === "Escape" && !modal.hidden) {
        closeModal();
      }
    });

    page.dataset.boundEvents = "true";
  }
}

function initToolkitPage(page) {
  const phases = window.INTEP_TOOLKIT_DATA || [];
  const board = page.querySelector("[data-journey-board]");
  const materialsHost = page.querySelector("[data-additional-materials]");
  renderStats(page.querySelector("[data-stats]"), phases);
  renderBoard(board, phases);
  renderAdditionalMaterials(materialsHost, ADDITIONAL_MATERIALS);
  setupModal(page, phases, ADDITIONAL_MATERIALS);

  let resizeFrame = null;
  window.addEventListener("resize", () => {
    if (resizeFrame) {
      window.cancelAnimationFrame(resizeFrame);
    }

    resizeFrame = window.requestAnimationFrame(() => {
      renderBoard(board, phases);
    });
  });
}

document.querySelectorAll("[data-toolkit-page]").forEach(initToolkitPage);
